backend:
  name: git-gateway
  branch: main # promijeni ako ti je default drugačiji

media_folder: "static/uploads"
public_folder: "/uploads"

collections:
  - name: "site"
    label: "Site content"
    files:
      - label: "Naslovnica"
        name: "home"
        file: "data/home.json"
        fields:
          - { label: "Hero naslov", name: "heroTitle", widget: "string" }
          - { label: "Hero tekst", name: "heroText", widget: "text" }
